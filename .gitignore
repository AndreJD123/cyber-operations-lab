CLAUDE.md
venv/
aws/
awscliv2.zip
.env
__pycache__/
*.pyc
.vscode/
*.code-workspace
notes/progress-log.md
push.sh
docs/*worksheet*.md

# Separate git repo (own remote, own history) — not a submodule, ignored
# explicitly here rather than relying on git's default nested-repo handling.
python/soc-automation-toolkit/
